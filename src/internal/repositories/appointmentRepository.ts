// TODO: Keep data access logic encapsulated in repositories.
export class AppointmentRepository {
  public async create(): Promise<void> {
    // TODO: use Prisma client to persist appointment data.
  }
}
